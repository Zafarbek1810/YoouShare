import React from 'react';
import Header from '../Header/Header';

const Contacts = () => {
    return (
        <div>
            <Header/>
            contacts
        </div>
    );
};

export default Contacts;